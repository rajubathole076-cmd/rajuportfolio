// Supabase client stub — NOT connected in Stage A.
//
// The Supabase client will be initialized here during Stage F
// only after verifying:
//   1. The Supabase configuration is operational
//   2. The contact_submissions table exists or can be created
//   3. Row Level Security is enabled
//   4. Public/anon users can ONLY insert (not read/update/delete)
//   5. No service-role keys are exposed to the browser
//
// If any check fails, the fallback email-service approach will be used.

export {};
